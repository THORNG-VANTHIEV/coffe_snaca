name: Deploy menu to GitHub Pages

# Every push to main republishes the menu — this is the shop owner's
# "publish" button (spec §54, §55).
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  # Required by actions/deploy-pages v4 and later, which fetches the uploaded
  # artifact through the Actions API.
  actions: read

# Never let two deploys race; queue instead, and let a newer push win.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Work out the base path
        # A project site is served from /<repo>/, a user or org site from /.
        # Deriving it here means renaming the repository does not break assets.
        run: |
          repo="${GITHUB_REPOSITORY#*/}"
          if [[ "$repo" == *.github.io ]]; then
            echo "VITE_BASE=/" >> "$GITHUB_ENV"
          else
            echo "VITE_BASE=/$repo/" >> "$GITHUB_ENV"
          fi

      - name: Check menu data, contrast, types and lint
        run: npm run check

      - name: Build
        run: npm run build

      # Fails with "Get Pages site failed … Not Found" until Pages is turned on
      # for the repository: Settings → Pages → Source: GitHub Actions.
      - uses: actions/configure-pages@v6

      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
